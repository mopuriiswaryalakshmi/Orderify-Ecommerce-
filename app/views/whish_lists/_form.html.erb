<%= form_for(@whish_list) do |f| %>
  <% if @whish_list.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@whish_list.errors.count, "error") %> prohibited this whish_list from being saved:</h2>

      <ul>
      <% @whish_list.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= f.label :user_id %><br>
    <%= f.number_field :user_id %>
  </div>
  <div class="field">
    <%= f.label :product_id %><br>
    <%= f.number_field :product_id %>
  </div>
  <div class="actions">
    <%= f.submit %>
  </div>
<% end %>
